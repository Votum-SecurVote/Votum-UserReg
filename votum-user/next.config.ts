/**
 * Next.js Configuration.
 * Defines core settings for the Next.js application, including output mode.
 */
import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  output: "standalone",
}

export default nextConfig
